\* TLC configuration for MaceStoreTargeted model
\* Fix: targeted cache invalidation via PID dependency tracking.
\* On termination, only invalidates cache entries that depended on the dead PID.
\* First-config-wins invariants excluded — still the original fetch algorithm.
\*
\* Run with: make MaceStoreTargeted.cfg

SPECIFICATION Spec

CONSTANTS
  Pids   = {p1, p2, p3}
  Apps   = {a1}
  Keys   = {k1, k2}
  Values = {v1, v2}

INVARIANTS
  TypeOK
  \* LinkInheritance     \* first-config-wins — needs fallthrough fix
  \* MonitorInheritance  \* first-config-wins — needs fallthrough fix
  Isolation
  \* NoFalseNegatives    \* first-config-wins — needs fallthrough fix
  TerminationPermanent
  NoStaleCache           \* targeted invalidation fixes this
